apiVersion: v2
name: loki
description: Umbrella over upstream `loki` (naphtha defaults)
type: application
version: 1.0.0
appVersion: "6.55.0"

dependencies:
  - name: loki
    version: "6.55.0"
    repository: "https://grafana.github.io/helm-charts"
