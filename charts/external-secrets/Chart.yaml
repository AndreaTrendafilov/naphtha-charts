apiVersion: v2
name: external-secrets
description: Umbrella over upstream `external-secrets` (naphtha defaults)
type: application
version: 1.0.0
appVersion: "2.3.0"

dependencies:
  - name: external-secrets
    version: "2.3.0"
    repository: "https://charts.external-secrets.io"
