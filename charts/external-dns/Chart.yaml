apiVersion: v2
name: external-dns
description: Umbrella over upstream `external-dns` (naphtha defaults)
type: application
version: 1.0.0
appVersion: "1.20.0"

dependencies:
  - name: external-dns
    version: "1.20.0"
    repository: "https://kubernetes-sigs.github.io/external-dns/"
