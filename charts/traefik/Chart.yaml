apiVersion: v2
name: traefik
description: Umbrella over upstream `traefik` (naphtha defaults)
type: application
version: 1.0.0
appVersion: "39.0.8"

dependencies:
  - name: traefik
    version: "39.0.8"
    repository: "https://traefik.github.io/charts"
