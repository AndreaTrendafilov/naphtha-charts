apiVersion: v2
name: tempo
description: Umbrella over upstream `tempo` (naphtha defaults)
type: application
version: 1.0.0
appVersion: "1.24.4"

dependencies:
  - name: tempo
    version: "1.24.4"
    repository: "https://grafana.github.io/helm-charts"
